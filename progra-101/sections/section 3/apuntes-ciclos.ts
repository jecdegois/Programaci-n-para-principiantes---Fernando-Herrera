// Ciclo While es util cuando no se sabe
// exactamente cuantas veces de se debe ejecutar el codigo

// Mientras la condicion sea true, se ejecuta


// Ciclo For es muy util cuando sabemos la cantidad de veces que necesitamos que se ejecute el codigo
//La variable de control puede ser cualquier numero

//El for esta compuesto por:
//variable de control, condicion e incrementador

for(let variableDeControl = 1; variableDeControl <= 0; variableDeControl++ )
{
    console.log(variableDeControl)
}

// El codigo de arriba se ejecuta 10 veces